var expect = require('chai').expect;

var xbeforeEach = function(){};

describe('REST API', function(){

  beforeEach(function(done){

    done();

  });

  it('Can do something', function(done) {
      expect(1).to.equal(404);
  });

});

